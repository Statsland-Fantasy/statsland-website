name: Deploy to Dev

on:
  push:
    branches:
      - 'release/**'
      - 'release-*'
  workflow_dispatch: # Allow manual trigger
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging

jobs:
  build-and-deploy:
    name: Build and Deploy to Dev
    runs-on: ubuntu-latest
    environment:
      name: dev
      # url: https://dev.statsland.com # Uncomment and update with your dev URL

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: CI=true npm test -- --passWithNoTests
        env:
          CI: true

      - name: Build for production
        run: npm run build
        env:
          CI: false
          REACT_APP_ENV: dev
          # Add your dev environment variables here:
          # REACT_APP_API_URL: ${{ secrets.DEV_API_URL }}

      - name: Run smoke tests
        run: |
          echo "Running smoke tests..."
          # Add smoke test commands here
          # e.g., check if build/index.html exists
          test -f build/index.html && echo "✅ Build output verified"

      # Option 1: Deploy to Vercel
      # Uncomment and configure if using Vercel
      # - name: Deploy to Vercel
      #   uses: amondnet/vercel-action@v25
      #   with:
      #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
      #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
      #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
      #     vercel-args: '--prod'
      #     working-directory: ./

      # Option 2: Deploy to Netlify
      # Uncomment and configure if using Netlify
      # - name: Deploy to Netlify
      #   uses: nwtgck/actions-netlify@v3
      #   with:
      #     publish-dir: './build'
      #     production-deploy: false
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     deploy-message: "Deploy from GitHub Actions - ${{ github.ref_name }}"
      #   env:
      #     NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      #     NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      # Option 3: Deploy to AWS S3
      # Uncomment and configure if using AWS S3 + CloudFront
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: us-east-1
      #
      # - name: Deploy to S3
      #   run: |
      #     aws s3 sync build/ s3://your-dev-bucket --delete
      #     aws cloudfront create-invalidation --distribution-id YOUR_DIST_ID --paths "/*"

      # Placeholder deployment step - replace with actual deployment
      - name: Deployment (Configure Me!)
        run: |
          echo "🚀 Deployment step placeholder"
          echo "📦 Build ready in ./build directory"
          echo "⚠️  Configure deployment by uncommenting one of the deployment options above"
          echo ""
          echo "Available deployment options:"
          echo "  1. Vercel"
          echo "  2. Netlify"
          echo "  3. AWS S3 + CloudFront"
          echo "  4. Custom deployment script"

      - name: Post-deployment health check
        run: |
          echo "Running post-deployment checks..."
          # Add health check commands here
          # e.g., curl https://dev.statsland.com/health
          echo "✅ Health check passed (configure actual endpoint)"

      - name: Create deployment summary
        run: |
          echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** Dev" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Build Info" >> $GITHUB_STEP_SUMMARY
          BUILD_SIZE=$(du -sh build | cut -f1)
          echo "- Build size: $BUILD_SIZE" >> $GITHUB_STEP_SUMMARY
          echo "- Node version: 18" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⚠️ **Configure deployment target in workflow file**" >> $GITHUB_STEP_SUMMARY

  notify:
    name: Send Notifications
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: always()

    steps:
      # Option: Slack notification
      # - name: Slack Notification
      #   uses: 8398a7/action-slack@v3
      #   with:
      #     status: ${{ needs.build-and-deploy.result }}
      #     text: 'Deployment to Dev: ${{ needs.build-and-deploy.result }}'
      #     webhook_url: ${{ secrets.SLACK_WEBHOOK }}
      #   if: always()

      - name: Summary
        run: |
          echo "Deployment ${{ needs.build-and-deploy.result }}"
          echo "Configure notification service (Slack, Discord, etc.) above"
